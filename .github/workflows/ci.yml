name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

jobs:
  content-validate:
    name: Content
    uses: ./.github/workflows/reusable-content-validate.yaml
